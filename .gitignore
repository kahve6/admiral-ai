# gstack workspace cache
.gstack/

# Deploy / FTP sync artifacts
.ftp-deploy-sync-state.json
*.log

# Runtime user datastore (lives on the host, never in git)
data/users.json
data/ads_*.json
data/competitors_*.json

# Local FTP sync credentials (see .ftp-sync.example.json for the template)
.ftp-sync.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (none used today, but keep clean)
node_modules/
