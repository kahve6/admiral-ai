# gstack workspace cache
.gstack/

# Deploy / FTP sync artifacts
.ftp-deploy-sync-state.json
*.log

# Runtime datastore (user data lives on the host, never in git)
storage/*.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (none used today, but keep clean)
node_modules/
