# gstack workspace cache
.gstack/

# Deploy / FTP sync artifacts
.ftp-deploy-sync-state.json
*.log

# Runtime user datastore (lives on the host, never in git)
data/users.json

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Node (none used today, but keep clean)
node_modules/
